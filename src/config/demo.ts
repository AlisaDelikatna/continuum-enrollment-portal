/**
 * Demo switches. These exist so a live walkthrough is smooth; none of them
 * belong in a real deployment.
 */

/**
 * When false, the public enrollment form blocks nothing — you can click
 * "Continue to documents" with an empty form and the server backfills
 * placeholders. Set to true to restore normal validation (name and a valid
 * email required, at least one waiver program).
 */
export const REQUIRE_COMPLETE_ENROLLMENT_FORM = false;
